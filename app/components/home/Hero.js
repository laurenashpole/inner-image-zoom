import { Container, Title } from '@mantine/core';

const Hero = () => {
  return (
    <Container p="md">
      <Title order={1}>Inner Image Zoom</Title>
    </Container>
  );
};

export default Hero;

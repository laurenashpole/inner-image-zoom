import DocsView from '@/app/components/docs/DocsView';

import { DATA } from './data';

export const metadata = {
  title: 'Inner Image Zoom | Docs | React v1.3.0',
};

const React = () => <DocsView docs={DATA} />;

export default React;
